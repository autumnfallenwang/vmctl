# testenv/corpus

Captured golden IG log samples used by the replay engine and as fixtures for
vmctl's tests. Dev-only — never shipped in the wheel.
See [ADR 0002](../../docs/adr/0002-dev-test-infra.md).
